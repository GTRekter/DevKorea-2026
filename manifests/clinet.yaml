apiVersion: v1
kind: Pod
metadata:
  name: curl
  namespace: simple-app
  labels:
    app: curl
  annotations:
    linkerd.io/inject: disabled
spec:
  containers:
    - name: curl
      image: curlimages/curl:latest
      command: ["sleep", "infinity"]
